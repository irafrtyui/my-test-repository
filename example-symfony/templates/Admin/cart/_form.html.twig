{{ form_start(form) }}
    {{ form_widget(form) }}
<div class="form-group row">
    <div class="col-sm-4 col-sm-offset-2">
        <a class="btn btn-white btn-sm" href="{{ path('app_admin_cart_index') }}">back to list</a>
        <button class="btn btn-primary btn-sm" type="submit">{{ button_label|default('Save') }}</button>
        {{ include('admin/cart/_delete_form.html.twig') }}
    </div>
</div>
{{ form_end(form) }}
